#include <iostream>

enum TOKEN {
    PROG_BEGIN,   
    DECLARE, 
    VAR_NAME, 
    ASSIGN,
    INPUT,
    OUTPUT,
    IF,
    IF_STATEMENT,
    LOGICAL_EXPRESSION,
    ELSE,
    ELSE_STATEMENT,
    ENDL,
    INTEGER,
    DOUBLE,
    STRING,
};